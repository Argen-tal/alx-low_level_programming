#include "main.h"

/**
 * _isupper - Entry point
 * _isupper: The function checks for uppercase letters
 * Return: Always 0  (success)
 */

int _isupper(int c)
{
	if (c = 'A'; c <= 'Z'; c++)
	{
		_putchar(1);
	}
	else
	{
		_putchar(0);
	}
	_putchar('\n');
}
